# Redis configuration for local container usage.
bind 0.0.0.0
port 6379
protected-mode yes

# Persistence tuned for session-style caching so restarts do not wipe data.
appendonly yes
appendfsync everysec

# Password is injected at runtime via the REDIS_PASSWORD environment variable.
# requirepass <password>
