const CONTACT_MESSAGE_STATUSES = Object.freeze(['new', 'seen', 'responded', 'closed']);

const CONTACT_TOPICS = Object.freeze([
  'General enquiry',
  'Sales & onboarding',
  'Verification help',
  'Billing',
  'Report an issue',
]);

module.exports = { CONTACT_MESSAGE_STATUSES, CONTACT_TOPICS };
