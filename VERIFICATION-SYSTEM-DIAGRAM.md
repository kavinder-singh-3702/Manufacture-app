# 📊 Verification System - Visual Diagrams

## 🏗️ System Architecture

```
┌──────────────────────────────────────────────────────────────────────┐
│                         USER'S PHONE/BROWSER                         │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │  Profile Page  │  │ Verify Form    │  │ Admin Panel    │        │
│  │  (Yellow/Green │  │ (GST + Bank)   │  │ (Pending List) │        │
│  │   Border)      │  │                │  │                │        │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘        │
│           │                   │                   │                 │
└───────────┼───────────────────┼───────────────────┼─────────────────┘
            │                   │                   │
            │ HTTP Requests     │                   │
            ▼                   ▼                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         EXPRESS BACKEND SERVER                       │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                   ROUTES (URL Mapping)                      │    │
│  │                                                             │    │
│  │  GET  /api/verification/status          → getStatus()      │    │
│  │  POST /api/verification/submit          → submitVerify()   │    │
│  │  POST /api/verification/upload          → uploadDoc()      │    │
│  │  GET  /api/verification/admin/pending   → getPending()     │    │
│  │  POST /api/verification/admin/approve   → approve()        │    │
│  │  POST /api/verification/admin/reject    → reject()         │    │
│  └───────────────────────────┬─────────────────────────────────┘    │
│                              │                                       │
│  ┌───────────────────────────▼─────────────────────────────────┐    │
│  │             CONTROLLERS (Business Logic)                    │    │
│  │                                                             │    │
│  │  • Validate input data                                     │    │
│  │  • Check user permissions                                  │    │
│  │  • Process verification requests                           │    │
│  │  • Update database                                         │    │
│  │  • Send responses                                          │    │
│  └───────────────────────────┬─────────────────────────────────┘    │
│                              │                                       │
└──────────────────────────────┼───────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         MONGODB DATABASE                             │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  Users Collection                                          │     │
│  │                                                            │     │
│  │  {                                                         │     │
│  │    _id: "abc123",                                          │     │
│  │    email: "john@example.com",                              │     │
│  │    displayName: "John Doe",                                │     │
│  │    verificationStatus: "pending",          ← NEW FIELD     │     │
│  │    verificationData: {                     ← NEW FIELD     │     │
│  │      gstNumber: "22AAAAA0000A1Z5",                         │     │
│  │      accountNumber: "1234567890",                          │     │
│  │      ifscCode: "SBIN0001234",                              │     │
│  │      accountHolderName: "John Doe",                        │     │
│  │      gstDocumentUrl: "https://...",                        │     │
│  │      bankProofUrl: "https://...",                          │     │
│  │      submittedAt: "2025-01-15T10:30:00Z",                  │     │
│  │      reviewedAt: null,                                     │     │
│  │      reviewedBy: null,                                     │     │
│  │      rejectionReason: null                                 │     │
│  │    }                                                       │     │
│  │  }                                                         │     │
│  └────────────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete User Flow Diagram

```
START: User Signs Up
         │
         ▼
┌─────────────────────────┐
│ User Account Created    │
│ Status: "unverified"    │
│ Border: YELLOW 🟡       │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│ User Opens Profile      │
│ Sees "Verify Account"   │
│ Button                  │
└──────────┬──────────────┘
           │
           │ Clicks "Verify Account"
           ▼
┌─────────────────────────────────────────┐
│ Verification Form Appears               │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ GST Number: [____________]          │ │
│ │ Account Number: [____________]      │ │
│ │ IFSC Code: [____________]           │ │
│ │ Account Holder: [____________]      │ │
│ │                                     │ │
│ │ Upload GST Certificate [Choose]     │ │
│ │ Upload Bank Proof [Choose]          │ │
│ │                                     │ │
│ │         [Submit Request]            │ │
│ └─────────────────────────────────────┘ │
└──────────┬──────────────────────────────┘
           │
           │ User submits form
           ▼
┌─────────────────────────┐
│ POST /api/verification  │
│      /submit            │
│                         │
│ Backend validates &     │
│ saves to MongoDB        │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│ Status: "pending"       │
│ Border: BLUE 🔵         │
│                         │
│ Message: "Under Review" │
└──────────┬──────────────┘
           │
           │ Admin receives notification
           ▼
┌─────────────────────────────────────────┐
│ ADMIN DASHBOARD                         │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Pending Verifications (1)           │ │
│ │                                     │ │
│ │ ┌───────────────────────────────┐   │ │
│ │ │ John Doe                      │   │ │
│ │ │ GST: 22AAAAA0000A1Z5          │   │ │
│ │ │ Account: 1234567890           │   │ │
│ │ │ IFSC: SBIN0001234             │   │ │
│ │ │                               │   │ │
│ │ │ [View Docs] [Approve] [Reject]│   │ │
│ │ └───────────────────────────────┘   │ │
│ └─────────────────────────────────────┘ │
└──────────┬──────────────────────────────┘
           │
           │
      ┌────┴────┐
      │         │
      ▼         ▼
┌──────────┐  ┌──────────┐
│ APPROVE  │  │ REJECT   │
└────┬─────┘  └────┬─────┘
     │             │
     ▼             ▼
┌─────────────┐  ┌─────────────┐
│ Status:     │  │ Status:     │
│ "verified"  │  │ "rejected"  │
│             │  │             │
│ Border:     │  │ Border:     │
│ GREEN 🟢    │  │ RED 🔴      │
│             │  │             │
│ Badge: ✓    │  │ Can retry   │
└─────────────┘  └─────────────┘
```

---

## 📡 API Request/Response Flow

### Example: User Submits Verification

```
┌──────────────────────────────────────────────────────────────┐
│ STEP 1: Frontend sends HTTP request                         │
└──────────────────────────────────────────────────────────────┘

POST /api/verification/submit
Headers:
  Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  Content-Type: application/json

Body:
{
  "gstNumber": "22AAAAA0000A1Z5",
  "accountNumber": "1234567890",
  "ifscCode": "SBIN0001234",
  "accountHolderName": "John Doe"
}

         ▼

┌──────────────────────────────────────────────────────────────┐
│ STEP 2: Express receives & routes request                   │
└──────────────────────────────────────────────────────────────┘

router.post('/submit', submitVerification);
                       ↑
                       └── This function handles it

         ▼

┌──────────────────────────────────────────────────────────────┐
│ STEP 3: Controller validates input                          │
└──────────────────────────────────────────────────────────────┘

submitVerification(req, res, next) {
  const { gstNumber, accountNumber, ifscCode, accountHolderName } = req.body;

  // Check all fields present
  if (!gstNumber || !accountNumber || !ifscCode || !accountHolderName) {
    throw Error("All fields required");
  }

  // Get user from token
  const userId = req.user._id;
  const user = await User.findById(userId);

  // Check not already verified
  if (user.verificationStatus === 'verified') {
    throw Error("Already verified");
  }

         ▼

┌──────────────────────────────────────────────────────────────┐
│ STEP 4: Update MongoDB                                      │
└──────────────────────────────────────────────────────────────┘

  user.verificationStatus = 'pending';
  user.verificationData = {
    gstNumber,
    accountNumber,
    ifscCode,
    accountHolderName,
    submittedAt: new Date()
  };

  await user.save();

         ▼

┌──────────────────────────────────────────────────────────────┐
│ STEP 5: Send response back to frontend                      │
└──────────────────────────────────────────────────────────────┘

  res.status(200).json({
    success: true,
    message: "Verification request submitted successfully",
    data: {
      status: "pending",
      submittedAt: "2025-01-15T10:30:00.000Z"
    }
  });
}

         ▼

┌──────────────────────────────────────────────────────────────┐
│ STEP 6: Frontend receives response & updates UI             │
└──────────────────────────────────────────────────────────────┘

const response = await fetch('/api/verification/submit', ...);
const data = await response.json();

if (data.success) {
  // Update UI
  setBorderColor('blue');
  showNotification('Verification submitted! Under review.');
}
```

---

## 🎨 UI State Management

```
┌────────────────────────────────────────────────────────────────┐
│                   VERIFICATION STATUS                          │
└────────────────────────────────────────────────────────────────┘

unverified                 pending
    │                         │
    │                         │
    ▼                         ▼
┌─────────┐              ┌─────────┐
│ YELLOW  │              │  BLUE   │
│ BORDER  │              │ BORDER  │
│         │              │         │
│    ?    │              │    ⏳   │
└─────────┘              └─────────┘
    │                         │
    │                         │
    │                    ┌────┴────┐
    │                    │         │
    │                    ▼         ▼
    │               approved   rejected
    │                    │         │
    │                    ▼         ▼
    │               ┌─────────┐ ┌─────────┐
    │               │  GREEN  │ │   RED   │
    │               │ BORDER  │ │ BORDER  │
    │               │         │ │         │
    │               │    ✓    │ │    ✗    │
    │               └─────────┘ └────┬────┘
    │                              │
    └──────────────────────────────┘
              User can resubmit
```

---

## 🗂️ Database Structure

```
MongoDB Database: manufacture_db
│
├── Collection: users
│   │
│   ├── Document 1
│   │   ├── _id: ObjectId("abc123")
│   │   ├── email: "john@example.com"
│   │   ├── displayName: "John Doe"
│   │   ├── role: "user"
│   │   ├── verificationStatus: "pending"        ← NEW
│   │   └── verificationData:                    ← NEW
│   │       ├── gstNumber: "22AAAAA0000A1Z5"
│   │       ├── accountNumber: "1234567890"
│   │       ├── ifscCode: "SBIN0001234"
│   │       ├── accountHolderName: "John Doe"
│   │       ├── gstDocumentUrl: "https://..."
│   │       ├── bankProofUrl: "https://..."
│   │       ├── submittedAt: ISODate("2025-01-15")
│   │       ├── reviewedAt: null
│   │       ├── reviewedBy: null
│   │       └── rejectionReason: null
│   │
│   ├── Document 2
│   │   ├── _id: ObjectId("xyz789")
│   │   ├── email: "admin@example.com"
│   │   ├── displayName: "Admin User"
│   │   ├── role: "admin"                        ← Can approve/reject
│   │   └── ...
│   │
│   └── ...
│
└── Collection: companies
    └── ...
```

---

## 🔐 Security Flow

```
┌────────────────────────────────────────────────────────────────┐
│                    REQUEST SECURITY FLOW                       │
└────────────────────────────────────────────────────────────────┘

User sends request
        │
        ▼
┌──────────────────┐
│ Has JWT Token?   │
│ (Authorization)  │
└────┬────────┬────┘
     │ NO     │ YES
     ▼        ▼
┌─────────┐  ┌──────────────────┐
│ Return  │  │ Verify JWT Token │
│ 401     │  │ Valid?           │
│ Error   │  └────┬────────┬────┘
└─────────┘       │ NO     │ YES
                  ▼        ▼
              ┌─────────┐  ┌──────────────────┐
              │ Return  │  │ Extract user ID  │
              │ 401     │  │ from token       │
              │ Error   │  └────┬─────────────┘
              └─────────┘       │
                                ▼
                        ┌──────────────────┐
                        │ Admin route?     │
                        └────┬────────┬────┘
                             │ NO     │ YES
                             ▼        ▼
                        ┌─────────┐  ┌──────────────┐
                        │ Process │  │ Check role   │
                        │ Request │  │ role=='admin'│
                        └─────────┘  └────┬────┬────┘
                                          │NO  │YES
                                          ▼    ▼
                                      ┌────┐ ┌────┐
                                      │403 │ │ ✓  │
                                      └────┘ └────┘
```

---

## 📊 Admin Dashboard Structure

```
┌────────────────────────────────────────────────────────────────┐
│                        ADMIN DASHBOARD                         │
└────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  STATISTICS                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │Unverified│  │ Pending  │  │ Verified │  │ Rejected │     │
│  │  1,250   │  │    35    │  │   580    │  │    15    │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  PENDING VERIFICATIONS (35)                                  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🔵 John Doe                          john@example.com │  │
│  │    GST: 22AAAAA0000A1Z5                               │  │
│  │    Account: 1234567890  IFSC: SBIN0001234             │  │
│  │    Submitted: 2 hours ago                             │  │
│  │    [View Docs] [✓ Approve] [✗ Reject]                 │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🔵 Jane Smith                      jane@example.com   │  │
│  │    GST: 22BBBBB0000B1Z5                               │  │
│  │    Account: 9876543210  IFSC: HDFC0001234             │  │
│  │    Submitted: 5 hours ago                             │  │
│  │    [View Docs] [✓ Approve] [✗ Reject]                 │  │
│  └────────────────────────────────────────────────────────┘  │
│  ...                                                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                  VERIFICATION SYSTEM OVERVIEW                   │
└─────────────────────────────────────────────────────────────────┘

FRONTEND (React Native)
├── Components
│   ├── Avatar.tsx (Yellow/Green border)
│   ├── VerificationForm.tsx (Submit GST + Bank)
│   ├── DocumentUpload.tsx (Upload files)
│   └── AdminDashboard.tsx (Review requests)
│
BACKEND (Express + MongoDB)
├── Models
│   └── user.model.js (verificationStatus + verificationData)
├── Controllers
│   └── verification.controller.js (7 endpoint handlers)
├── Routes
│   └── verification.routes.js (URL mappings)
└── Middleware
    ├── auth.js (JWT verification)
    └── admin.js (Role check)

DATABASE (MongoDB)
└── users collection
    └── New fields: verificationStatus, verificationData

FEATURES
├── User can submit verification
├── Status: unverified → pending → verified/rejected
├── Visual indicator: Yellow → Blue → Green/Red border
├── Admin can approve/reject
├── Document upload support
└── Audit trail (who reviewed, when)
```

---

This visual guide should help you understand how all the pieces fit together! 🎉
